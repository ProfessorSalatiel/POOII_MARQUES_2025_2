<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <connectionStrings>
	<!-- SQL Server LocalDB (laboratório / ambiente de desenvolvimento)
         Instala junto com o Visual Studio -->
	  <add name="SqlLab"
		connectionString="Data Source=(localdb)\MSSQLLocalDB;Initial Catalog=POOII_Lab;Integrated Security=True;Encrypt=True;TrustServerCertificate=True;" />

    <!-- SQL Server Express (quando instalado no laboratório com instância nomeada) -->
    <add name="SqlExpress"
         connectionString="Data Source=.\SQLEXPRESS;Initial Catalog=POOII_Lab;Integrated Security=True;Encrypt=True;TrustServerCertificate=True;" />

	<!-- SQL Server padrão local (instância default) -->
    <add name="SqlServerLocal"
         connectionString="Server=localhost;Database=POOII_Lab;Integrated Security=true;" />

    <!-- (Opcional) PostgreSQL via Npgsql (exemplo didático, requer driver Npgsql) -->
	  <add name="Postgres"
		 connectionString="Host=db.vwfntqecmdatvmnwvemn.supabase.co;Port=5432;Database=postgres;Username=postgres;Password=29082025@Unip;SSL Mode=Require;Trust Server Certificate=true;" />
  </connectionStrings>
  <startup>
    <supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.8"/>
  </startup>
</configuration>
